node{
   stage('BUILD'){
     sh "${mvn}/bin/mvn clean install"
     echo "BUILD IS SUCCESSFUL"
   }
   stage('DEPLOY'){
    sshagent(['jenkinstom']) {
    echo "deploy successful"
     }
    
    }
}   
